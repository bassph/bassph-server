module.exports = {
    serverPort: process.env.SERVER_PORT,
    mongoUrl: 'mongodb://localhost:27017/bassdb',
    dbName: 'bassdb'
    // other configs, like mongo connection, urls to other remote API's, etc
}